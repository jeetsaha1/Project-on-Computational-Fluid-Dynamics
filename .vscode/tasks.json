{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Fortran",
      "type": "shell",
      "command": "gfortran ${file} -o ${fileBasenameNoExtension}.exe && .\\${fileBasenameNoExtension}.exe",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"]
    }
  ]
}
